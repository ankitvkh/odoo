<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Enhanced Sale Order Form View -->
    <record id="view_order_form_enhanced" model="ir.ui.view">
        <field name="name">sale.order.form.enhanced</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            
            <!-- Enhance Order Lines Tree View - Add Tax Amount and Total columns -->
            <xpath expr="//field[@name='order_line']/list//field[@name='price_subtotal']" position="after">
                <field name="price_tax" string="Tax Amount" optional="show"/>
                <field name="price_total" string="Total" optional="show"/>
            </xpath>
            
            <!-- Add control buttons for Order Lines -->
            <xpath expr="//field[@name='order_line']" position="attributes">
                <attribute name="context">{
                    'default_order_id': id,
                    'tree_view_ref': 'sale_order_enhancement.view_order_line_tree_enhanced',
                }</attribute>
            </xpath>
            
            <!-- Add totals summary below Order Lines -->
            <xpath expr="//field[@name='order_line']" position="after">
                <group class="oe_subtotal_footer oe_right" colspan="2" name="sale_total">
                    <field name="amount_untaxed_display"/>
                    <field name="amount_tax_display"/>
                    <field name="amount_total_display" class="oe_subtotal_footer_separator"/>
                </group>
            </xpath>
            
            <!-- Enhance Optional Products Tab if it exists -->
            <xpath expr="//field[@name='sale_order_option_ids']/list//field[@name='price_unit']" position="after">
                <field name="price_tax" optional="show" string="Tax Amount"/>
                <field name="price_total" optional="show" string="Total"/>
            </xpath>
            
            <!-- Add totals summary below Optional Products -->
            <xpath expr="//page[@name='optional_products']" position="inside">
                <group class="oe_subtotal_footer oe_right" colspan="2" name="sale_total_option">
                    <field name="amount_untaxed_display"/>
                    <field name="amount_tax_display"/>
                    <field name="amount_total_display" class="oe_subtotal_footer_separator"/>
                </group>
            </xpath>
            
            <!-- Add custom reference code field in partner details -->
            <xpath expr="//group[@name='partner_details']/field[@name='partner_id']" position="before">
                <field name="custom_ref_code" 
                       placeholder="Reference Code (XXXXX)"
                       readonly="state not in ['draft', 'sent']"/>
                <field name="offer_type" widget="selection" readonly="state not in ['draft', 'sent']"/>
            </xpath>
            
        </field>
    </record>
    
    <!-- Enhanced Order Line Tree View with Section/Note/Catalog options -->
    <record id="view_order_line_tree_enhanced" model="ir.ui.view">
        <field name="name">sale.order.line.tree.enhanced</field>
        <field name="model">sale.order.line</field>
        <field name="arch" type="xml">
            <list string="Order Lines" editable="bottom">
                <control>
                    <create string="Add a product"/>
                    <create string="Add a section" context="{'default_display_type': 'line_section'}"/>
                    <create string="Add a note" context="{'default_display_type': 'line_note'}"/>
                </control>
                
                <field name="display_type" column_invisible="True"/>
                <field name="sequence" widget="handle"/>
                
                <!-- Section/Note display -->
                <field name="name" widget="section_and_note_text" optional="show"/>
                
                <!-- Product fields -->
                <field name="product_id" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       context="{'partner_id': parent.partner_id, 'quantity': product_uom_qty, 'pricelist': parent.pricelist_id, 'company_id': parent.company_id}"
                       readonly="state in ('sale', 'done', 'cancel')"
                       required="not display_type"
                       domain="[('sale_ok', '=', True)]"/>
                
                <field name="product_template_id" column_invisible="True"/>
                
                <field name="product_uom_qty" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       readonly="state in ('sale', 'done', 'cancel')"
                       context="{'partner_id': parent.partner_id, 'quantity': product_uom_qty, 'pricelist': parent.pricelist_id, 'uom': product_uom, 'company_id': parent.company_id}"/>
                
                <field name="product_uom" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       readonly="state in ('sale', 'done', 'cancel')"
                       groups="uom.group_uom"/>
                
                <field name="price_unit" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       readonly="state in ('sale', 'done', 'cancel')"/>
                
                <field name="tax_id" 
                       widget="many2many_tags" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       readonly="state in ('sale', 'done', 'cancel')"
                       context="{'active_test': True}"
                       domain="[('type_tax_use', '=', 'sale'), ('company_id', '=', parent.company_id)]"/>
                
                <field name="discount" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       readonly="state in ('sale', 'done', 'cancel')"
                       groups="product.group_discount_per_so_line"/>
                
                <field name="price_subtotal" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       readonly="1"/>
                
                <field name="price_tax" 
                       string="Taxes" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       readonly="1"/>
                
                <field name="price_total" 
                       string="Total" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       readonly="1"/>
                
                <field name="state" column_invisible="True"/>
                <field name="company_id" column_invisible="True"/>
                <field name="currency_id" column_invisible="True"/>
            </list>
        </field>
    </record>
    
    <!-- Enhanced Optional Products Tree View -->
    <record id="view_sale_order_option_tree_enhanced" model="ir.ui.view">
        <field name="name">sale.order.option.tree.enhanced</field>
        <field name="model">sale.order.option</field>
        <field name="arch" type="xml">
            <list string="Optional Products" editable="bottom">
                <control>
                    <create string="Add a product"/>
                    <create string="Add a section" context="{'default_display_type': 'line_section'}"/>
                    <create string="Add a note" context="{'default_display_type': 'line_note'}"/>
                </control>
                
                <field name="display_type" column_invisible="True"/>
                <field name="sequence" widget="handle"/>
                
                <!-- Section/Note display -->
                <field name="name" widget="section_and_note_text" optional="show"/>
                
                <!-- Product fields -->
                <field name="product_id" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       required="not display_type"
                       context="{'partner_id': parent.partner_id, 'quantity': quantity, 'pricelist': parent.pricelist_id, 'company_id': parent.company_id, 'uom': uom_id}"
                       readonly="0"
                       domain="[('sale_ok', '=', True)]"/>
                
                <field name="quantity" 
                       optional="show"
                       context="{'partner_id': parent.partner_id, 'quantity': quantity, 'pricelist': parent.pricelist_id, 'uom': uom_id, 'company_id': parent.company_id}"
                       readonly="0"
                       invisible="display_type in ('line_section', 'line_note')"/>
                
                <field name="uom_id" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       readonly="0"
                       groups="uom.group_uom"/>
                
                <field name="price_unit" 
                       optional="show"
                       readonly="0"
                       invisible="display_type in ('line_section', 'line_note')"/>
                
                <field name="tax_id" 
                       widget="many2many_tags" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       readonly="0"
                       context="{'active_test': True}"
                       domain="[('type_tax_use', '=', 'sale'), ('company_id', '=', parent.company_id)]"/>
                
                <field name="discount" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       readonly="0"
                       groups="product.group_discount_per_so_line"/>
                
                <field name="price_subtotal" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       readonly="1"/>
                
                <field name="price_tax" 
                       string="Taxes" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       readonly="0"/>
                
                <field name="price_total" 
                       string="Total" 
                       optional="show"
                       invisible="display_type in ('line_section', 'line_note')"
                       readonly="1"/>
            </list>
        </field>
    </record>
    
</odoo>
