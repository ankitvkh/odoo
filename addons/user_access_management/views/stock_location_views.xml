<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Add Organization Location field to Stock Location Form -->
    <record id="view_location_form_org_location" model="ir.ui.view">
        <field name="name">stock.location.form.org.location</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='company_id']" position="after">
                <field name="org_location_id" options="{'no_create': True}"/>
            </xpath>
        </field>
    </record>
    
</odoo>
